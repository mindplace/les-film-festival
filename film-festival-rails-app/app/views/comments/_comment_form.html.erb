<%= form_for :comment, :url => review_comments_path, html: { id: "comment_form", :method => :post} do |f| %>

  <%= render partial: "shared/error_messages", locals: {object: @comment} %>

  <%= f.label :body %>:
  <%= f.text_area :body %><br>

  <%= f.submit %>
<% end %>