<%= form_for :comment, url: {action: "create"}, html: { id: "comment_form"} do |f| %>

  <%= render partial: "shared/error_messages", locals: {object: @comment} %>

  <%= f.label :body %>:
  <%= f.text_area :body %><br>

  <%= f.submit %>
<% end %>